These files are older, project-specific Zotero exports kept for reference.

Default sample builds now use `examples/references/zotero-export.bib` or `examples/references/zotero-export.json`.

Use the files in this `custom/` directory only when you specifically want the earlier Japanese-focused sample data.

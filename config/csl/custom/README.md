This directory keeps project-specific CSL variants that are no longer the default sample style.

Default sample builds now use `config/csl/chicago-notes-bibliography.csl`, which is a general Zotero CSL.

Use `word-japanese-note.csl` only when you want the older custom Japanese note formatting.

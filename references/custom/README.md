This directory keeps the older project-specific bibliography and CSL samples.

The default web app flow now starts with:

- `references/zotero-export.json`
- `references/zotero-export.bib`
- `references/chicago-notes-bibliography.csl`

Use the files in `references/custom/` when you want to compare against the earlier Japanese-focused citation setup.
